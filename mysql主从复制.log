
数据库的主从复制
主从复制的原理：
主服务器将数据修改记录写入二进制日志，
备库I/O线程将主库上的日志复制到自己的中继日志中(relay log)，
备库SQL线程读取中继日志并执行其中的SQL语句，从而实现数据同步。
主从复制的配置步骤：
----------------------------------------------------------
    主服务器部分：
1.编辑配置文件,重启服务
vim /etc/my.cnf
[mysqld]
server-id=1
log-bin=mysql-bin
systemctl restart mysql
2.备份数据库
mysqldump -u root -p --all-databases --triggers --routines --events 
--single-transaction --master-data=2 --set-gtid-purged=ON > /tmp/full_backup.sql
3. 在主服务器上创建复制用户，并授权REPLICATION SLAVE权限。
CREATE USER 'repl'@'%' IDENTIFIED BY '密码';  mysql8.0版本创建用户必须指定密码
GRANT ALL PRIVILEGES ON *.* TO 'repl'@'%' 
flush privileges;
4.查看当前的二进制日志文件名和位置并记录下来。
SHOW MASTER STATUS;
----------------------------------------------------------
    备服务器部分：
1. 在备服务器上导入备份数据。
mysql -u root -p < /tmp/full_backup.sql
2.编辑配置文件,重启服务
vim /etc/my.cnf
[mysqld]
server-id=2
systemctl restart mysql
3. 在备服务器上配置复制参数，指定主服务器的IP地址、复制用户和密码，以及主服务器的日志文件名和位置。
CHANGE MASTER TO
MASTER_HOST='主服务器IP地址',
MASTER_USER='repl',
MASTER_PASSWORD='password',
MASTER_LOG_FILE='主服务器日志文件名',
MASTER_LOG_POS=主服务器日志位置;
4. 启动备服务器的复制线程。
START SLAVE;
5. 在备服务器上查看复制状态，确保复制正常运行。
SHOW SLAVE STATUS\G;
主从复制的注意事项：
1. 主从服务器的时钟应该同步，建议使用NTP服务。
2. 主从服务器的网络连接应该稳定，避免复制中断。
3. 主从服务器的MySQL版本应该兼容，建议使用相同版本。
4. 主从复制是异步的，主服务器的修改可能会有延迟才会反映到备服务器上。
5. 定期检查复制状态，及时处理复制错误。
